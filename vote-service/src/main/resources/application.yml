server:
  port: 9102
spring:
  application:
    name: vote-service
  datasource:
    # Connexion à localhost (votre machine)
    url: jdbc:postgresql://localhost:5432/vote_db
    # Mettez VOS identifiants PostgreSQL locaux ici :
    username: postgres
    password: omar123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
feign:
  httpclient:
    enabled: true

logging:
  level:
    feign: DEBUG
    org.apache.http: DEBUG